// Payment gateway removed. Payment records are managed by the admin directly.
// This hook is kept as a placeholder so existing imports do not break.
export function usePayment() {
  return { loading: false };
}
